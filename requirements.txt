# Core async & network
aiohttp==3.13.2
aiofiles==25.1.0

# CLI tools
click==8.3.1

# Utilities
psutil==7.1.3
python-dotenv==1.2.1
schedule==1.2.2
requests==2.32.5
typing_extensions==4.15.0

# Telegram Bot & QR code
pyTelegramBotAPI==4.29.1
qrcode==8.2
pypng==0.20220715.0
pillow==12.0.0

# Database
pymongo==4.15.4

# Hysteria2 API
hysteria2-api==0.1.3

# X-UI/3X-UI API
# py3xui>=0.1.0  # Опционально, используется только в старом xui_client.py как fallback
nest-asyncio>=1.6.0
httpx>=0.24.0  # Обязательно для нового официального API клиента

# Web panel (FastAPI stack)
fastapi==0.121.3
Jinja2==3.1.6
python-multipart==0.0.20
hypercorn==0.18.0
pydantic==2.12.4
pydantic-settings==2.12.0
annotated-types==0.7.0

# SSL / ACME management
# Note: certbot is used as a system command, not imported as Python module.
# It's installed via pip for certbot-dns-cloudflare plugin compatibility.
# Consider installing system certbot via apt/yum for production use.
certbot==5.1.0
